const header = {
    color: "blue",
    fontSize: "100px",
    textAlign: "left"
}

export default function InlineComponent() {
  return (
    <div>

      <h1 style = {header}>Some heading</h1>
    </div>
  );
}
